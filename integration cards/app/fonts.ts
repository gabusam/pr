import { Cormorant_Garamond } from 'next/font/google';

export const verlagBold = Cormorant_Garamond({
  subsets: ['latin'],
  weight: '700',
  variable: '--font-verlag-bold',
});

export const verlagBook = Cormorant_Garamond({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-verlag-book',
});